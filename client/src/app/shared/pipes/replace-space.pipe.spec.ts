import { ReplaseSpacePipe } from './replace-space.pipe';

describe('ReplaseSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaseSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
